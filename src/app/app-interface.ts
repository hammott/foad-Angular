export interface user {
    email: string;
    password: string;
    address: string;
}

export interface Iuser {
    name: string;
    age: number;
}

export interface ICarouselContext {
    $implicit : string;
}